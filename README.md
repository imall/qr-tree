# qr-tree

QR code 攤成等角地板，樹從中央長出來。純 canvas 2D，無 build。

**跑法**：用瀏覽器打開 `index.html`。改輸入框的文字，樹跟著換（seed = 文字 hash，同樣文字永遠同一棵樹）。

**原理**：QR 用最高容錯等級 H（可容忍約 30% 模組毀損），所以樹遮住的部分照樣掃得出來。QR 本身完全不變形——藝術層跟資料層是分開的兩件事。
